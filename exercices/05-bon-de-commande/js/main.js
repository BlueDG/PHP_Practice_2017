


$(function(){
    
    // Installation du gestionnaire d'événement sur l'événement keyUp sur le champ de recherche
    // keyup c'est quand on relache une touche du clavier
    $('#search').on('keyup', onKeyUpSearch);
});